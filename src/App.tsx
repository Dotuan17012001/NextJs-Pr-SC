import { useState } from "react";

import "./App.scss";


function App() {
  const [count, setCount] = useState(0);
  const [listName, setListName] = useState([])

  return (
    <>
     hello world app
    </>
  );
}

export default App;
